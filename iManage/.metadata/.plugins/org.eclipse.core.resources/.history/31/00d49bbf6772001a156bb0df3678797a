package com.iManage.Client;

import javax.ws.rs.client.Client;
import javax.ws.rs.client.ClientBuilder;
import javax.ws.rs.client.Entity;
import javax.ws.rs.client.WebTarget;
import javax.ws.rs.core.Form;
import javax.ws.rs.core.MediaType;

public class Login {
	public static void main(String args[]) {
		
		Login log = new Login();
		
		System.out.println( log.checkuserinDB("john","John1234") );
	}
	
	public boolean checkuserinDB(String user,String pass) {
		 Client client = ClientBuilder.newClient();
		 WebTarget target = client.target("http://localhost:8181").path("iManage").path("rest").path("login").path("user");
		 Form form = new Form();
		 form.param("user",user);
		 form.param("pass",pass);
		 boolean validUser = target.request(MediaType.APPLICATION_JSON).post(Entity.entity(form,MediaType.APPLICATION_FORM_URLENCODED_TYPE),Boolean.class);
		 
		return validUser;
	}
	
}
